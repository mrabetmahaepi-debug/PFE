# Frontend Features

Feature modules will be migrated here progressively.

Target shape:

```txt
feature-name/
  api/
  components/
  hooks/
  pages/
  schemas/
  types.ts
```

Existing pages and services remain in their legacy folders until each feature is
migrated and tested.
