import MemberSpaceOverview from '../components/MemberSpaceOverview';

/** Page Dashboard membre (/home) — Recent + Workload by Status. */
const MemberSpaceDashboard: React.FC = () => (
  <MemberSpaceOverview variant="dashboard" />
);

export default MemberSpaceDashboard;
