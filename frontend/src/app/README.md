# Frontend App Layer

This folder will host application-level wiring:

- route composition
- providers
- global error boundaries
- application shell configuration

Current migration status: `providers.tsx` is intentionally minimal so the
existing app behavior stays unchanged while the modern stack is introduced.
